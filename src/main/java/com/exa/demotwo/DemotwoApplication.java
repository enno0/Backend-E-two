package com.exa.demotwo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The DemotwoApplication class is the entry point of the Spring Boot application.
 * It contains the main method that starts the application.
 */
@SpringBootApplication
public class DemotwoApplication {

    /**
     * The main method that serves as the entry point for the Spring Boot application.
     *
     * @param args command-line arguments passed to the application
     */
    public static void main(String[] args) {
        SpringApplication.run(DemotwoApplication.class, args);
    }

}
